# Environment Variables
# Production builds: set VITE_API_URL to your deployed API (not localhost), or the app will treat it as local-only and poll /health from every client.
VITE_API_URL=http://localhost:5000/api
VITE_SOCKET_URL=http://localhost:5000
